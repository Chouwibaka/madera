{% extends 'base.html.twig' %}

{% block body %}
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-sm btn-success"  type="submit">Valider</button>
    {{ form_end(form) }}
{% endblock %}
